#include "station.h"
#include <cassert>
#include <chrono>
using namespace std;

template<typename T,std::size_t N>
constexpr std::size_t arraysize(T (&)[N])noexcept{
    return N;
}

int main(void)
{
    return 0;
}
